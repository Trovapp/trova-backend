global:
  scrape_interval: 5s

scrape_configs:
  - job_name: trova-backend
    metrics_path: /actuator/prometheus
    static_configs:
      # host.docker.internal: 컨테이너 안에서 호스트에 떠있는 Spring Boot(8080)에 접근하기 위함
      - targets: ["host.docker.internal:8080"]
